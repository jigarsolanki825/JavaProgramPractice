/*
package ExceptionEx;

public class ShoppingCartExceptionEx {

    private List<Item> items = new ArrayList<>();

    public void addItem(Item item) {
        try {
            items.add(item);
        } catch (RuntimeExceptio
        n e) {
            System.out.println("Error adding item: " + e.getMessage());
            // Handle the error, e.g., show an error message to the user or log the issue
        }
    }

    public void checkout() {
        try {
            // Process payment and update inventory
        } catch (PaymentProcessingException | InventoryUpdateException e) {
            System.out.println("Error during checkout: " + e.getMessage());
            // Handle the specific errors, e.g., inform the user about the payment issue
        }
    }
}
*/
